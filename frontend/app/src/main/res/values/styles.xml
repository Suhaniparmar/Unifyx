<resources>
    <!-- SplashActivity theme without action bar and with no icon -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/unifyx_logo</item> <!-- Add splash background -->
        <item name="android:icon">@null</item>
    </style>
</resources>
